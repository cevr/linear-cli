---
"@cvr/linear": minor
---

Add `linear issue files` and `linear file download` to list and download files uploaded to Linear. The token is sent only to `uploads.linear.app` and never to a redirect target.
